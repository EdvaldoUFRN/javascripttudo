const soma =(v1,v2)=>{return v1+v2}

console.log(soma(5,5))
//se for somente 1 parametro de entrada, não precisa colocar o parenteses.

const nome = n=>{return n }

console.log(nome('Bruno'))

const add = n=>n+10

console.log(add(10))

//se for para ter apenas uma operação simples, a gente não precisa utilizar as chaves {}
